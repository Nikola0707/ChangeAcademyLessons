const todos = [
  "Finish homework",
  "Buy groceries",
  "Walk the dog",
  "Write code",
  "Exercise",
];

// Lets learn some array methods

// Log the numbe of todos
console.log(`You have ${todos.length} todos left!`);
// log the first todo
console.log(todos[0]);

// log the last todo
console.log(todos[todos.length - 1]);

// log the second last todo
console.log(todos[todos.length - 2]);
